{
  "name": "parcel-processor",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@turf/turf": "^5.1.6",
    "archiver": "^4.0.1",
    "aws-sdk": "^2.685.0",
    "chalk": "^4.0.0",
    "chokidar": "^3.4.0",
    "config": "^3.3.1",
    "data-api-client": "^1.0.1",
    "gdal-next": "^1.0.3",
    "ndjson": "^1.5.0",
    "prompt": "^1.0.0",
    "skmeans": "^0.11.3",
    "unzipper": "^0.10.11"
  },
  "scripts": {
    "start-dev": "NODE_ENV=development node index_run_through_npm_scripts.js",
    "start-prod": "NODE_ENV=production node index_run_through_npm_scripts.js",
    "dangerous-clean-s3-dev": "aws s3 rm s3://raw-data-po --recursive && aws s3 rm s3://data-products-po --recursive && aws s3 rm s3://tile-server-po --recursive",
    "dangerous-clean-s3-prod": "aws s3 rm s3://raw-data-po-dev --recursive && aws s3 rm s3://data-products-po-dev --recursive && aws s3 rm s3://tile-server-po-dev --recursive"
  },
  "devDependencies": {
    "prettier": "2.0.5"
  }
}
