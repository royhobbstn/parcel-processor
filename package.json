{
  "name": "parcel-processor",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "archiver": "^4.0.1",
    "aws-sdk": "^2.685.0",
    "chalk": "^4.0.0",
    "chokidar": "^3.4.0",
    "config": "^3.3.1",
    "data-api-client": "^1.0.1",
    "gdal-next": "^1.0.3",
    "prompt": "^1.0.0",
    "unzipper": "^0.10.11"
  },
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dangerous-clean-db": "NODE_ENV=production node ./dangerous/clean-db.js",
    "dangerous-clean-s3": "aws s3 rm s3://raw-data-po --recursive && aws s3 rm s3://data-products-po --recursive"
  },
  "devDependencies": {
    "prettier": "2.0.5"
  }
}
